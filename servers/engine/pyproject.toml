[project]
name = "clawdnd-engine"
version = "0.0.1"
description = "ClawDnD game-engine MCP: authoritative D&D 5e state, dice, combat, encounters, XP, persistence."
requires-python = ">=3.11,<3.13"
dependencies = [
  "mcp>=1.2.0",
  "pydantic>=2.6",
]

[tool.uv]
package = false
