You are running an AUTOMATED QA PLAYTEST of the WorldOS plugin. Play a COMPLETE short session, acting as BOTH roles:

- the DUNGEON MASTER — use the clawdnd `dungeon-master` skill and the clawdnd-engine / clawdnd-rules / clawdnd-voice MCP tools for ALL mechanics. Every die roll, rule lookup, HP change, condition, attack, and state write MUST go through the tools — never invent a number or a rule. This tool-discipline is the single most important thing being tested.
- a TEST PLAYER controlling a level-1 character who makes plausible, sometimes-bold choices and actually role-plays a little.

Session to cover, in order, then STOP:
1. Start the `cellar-rats` adventure (start_adventure).
2. Create a level-1 player character (any class) with create_character; confirm the AI companion is in the party (get_state). Give the PC a voice_id.
3. look_around, then travel_to toward the cellar (follow the connected locations).
4. Have ONE social interaction with an NPC using social_check (and remember/set_attitude as appropriate).
5. Run the FIRST combat to its conclusion: start_combat, roll initiative, take turns with attack / next_turn / apply_damage / apply_healing, include at least one rules-correct attack roll and at least one condition OR saving throw OR death save, then end_combat and award_xp.
6. save (confirm state via get_state), then STOP.

Finally, write a concise POST-SESSION QA SELF-REPORT (5-10 lines):
- What worked well.
- Anything that felt wrong, broke, errored, or was awkward (tool gaps, confusing outputs, missing capabilities, rules mistakes).
- Whether EVERY mechanic was genuinely tool-sourced (call out any place you had to invent or work around).

Keep it brisk — this is a regression playtest, not a novel. The voice backend is `null` (silent) for this run; still call speak() where the DM would, that's expected and fine.
