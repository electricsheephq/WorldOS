You are running a QA PLAYTEST of WorldOS that VALIDATES the character-creation / combat-fidelity fixes end to end — starting gear (so AC and inventory agree), subclass resource pools (a Battle Master's Superiority Dice), Unarmored-Defense AC, and the new advance_time clock — by building a veteran, letting an in-world day pass, and then running a REAL fight where those numbers matter. You play BOTH roles, kept honest:
- the DUNGEON MASTER (clawdnd `dungeon-master` skill): source EVERY mechanic from the engine/rules tools — never narrate a hit, a miss, HP, AC, a resource spend, or a death you didn't get from a tool. Tool names are INVISIBLE to the player: present clean narration only, no validation tables appended to the story.
- a TEST PLAYER: a seasoned veteran dropped into post-BG3 Baldur's Gate, who will end up in a fight.

Drive this sequence (improvise within it, but HIT every beat so the fixes are exercised):
1. `start_world("baldurs-gate", ending="netherbrain-destroyed-heroes-live")`, `start_session`. Read the canon header.
2. CREATE A VETERAN BATTLE MASTER: `start_character(origin="veteran_l5", class_name="Fighter", subclass="Battle Master", ...)` with `apply_srd_defaults` and real ability scores (use 5e shorthand if you like). Then `get_character` and CONFIRM aloud (DM-side, briefly): the sheet now carries STARTING GEAR (armor + a weapon — AC and inventory must AGREE, not AC 16 with an empty pack), and the correct armor_class. Because Battle Master is a SUBCLASS the SRD tables don't seed, register the Superiority Dice yourself with the CORRECT level-scaled count: a level-5 Battle Master has FOUR — `set_class_resource(.., "superiority_dice", 4, "short", "d8")` (4 at levels 3–6, 5 at 7–14, 6 at 15+); the first fight must be able to track them.
3. Recruit a fitting canon companion (`recruit_companion`); if the prelude seeded them, go straight to recruit.
4. A LEGWORK BEAT WHERE TIME PASSES: the veteran spends a long city day chasing a lead (ask around the docks, read a ledger, watch a warehouse). When you NARRATE hours passing, MOVE THE CLOCK with `advance_time(phases=...)` or `advance_time(to="evening")` — don't let the prose say dusk while the engine still says morning. Use `skill_check` for any Perception/Investigation/Stealth (engine rolls the sheet modifier); `social_check` for influence/read vs an NPC.
5. THE FIGHT (headline): the lead turns violent — `spawn_monster` the opposition (a pack via `count`). Run REAL combat: `start_combat` (first combatant acts immediately), `attack` (auto-applies damage — do NOT also call apply_damage), Extra Attack as several `attack`s under ONE `use_action(kind=action)`, `next_turn` once per turn. SPEND a Superiority Die in the fiction (a Riposte / Precision / Trip maneuver) and back it with `use_resource(.., "superiority_dice", 1)` — confirm the pool decrements. On the companion's turn call `companion_suggest_action` and play it. `end_combat` (note any XP / level-up offer).
6. Keep it to ~10 beats. Every NPC SPEAKS; the world PUSHES BACK; resolve only what the player declared; no bare-tag sign-off.

The QUESTION this run answers: when a subclassed veteran is built and then FIGHTS, do the fixes hold — gear present so AC is justified, Superiority Dice trackable and spent through the engine, the clock advancing with the story, and the fight fully tool-sourced? Play it for real.
