# Blind panel set (#1556)

5 uniform 1344x768 plates scored blind by a 5-scorer Sonnet panel; the slot->arm mapping is in
`../panel_mapping.json` (held back from scorers during scoring).

- `image_1,2,4,5.png` — the four surviving challenger/anchor arms (committed).
- `image_3.png` — the DISGUISED in-band real-art CONTROL: a real Pillars of Eternity II: Deadfire
  screenshot (band [6.8, 9.2]). **Deliberately NOT committed (copyright)** — same convention as the
  #1553 bake-off, whose control slot image_2 was also uncommitted. Source on disk:
  `/Users/lume/worldos-session-notes/plate-sprint/armB/poe2_control.jpg`. The control validates the
  instrument: if its median lands in-band the panel is trustworthy.
